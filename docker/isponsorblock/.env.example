# isponsorblock/.env — copy to .env and fill in values; never commit .env

# LAN IP of this host — binds the service for local access only
# iSponsorBlockTV is LAN-only; no Tailscale sidecar
BIND_ADDR=192.168.x.x
